<div class="row">
  <% @movies.each do |movie| %>
    <div class="col-md-3 col-sm-6">
      <div class="thumbnail">
          <%= link_to image_tag(movie.image(:medium)), movie %>
      </div>
      <p style="text-align: center"><%= movie.title %></p>
    </div>
  <%end%>
</div>